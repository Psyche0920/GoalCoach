"""Model and workflow telemetry.

TODO(interface): record request correlation ID, component, model/provider, prompt version,
latency, input/output tokens, estimated cost, validation result, and fallback status.
Never log secrets or raw learner answers without an explicit privacy policy.
"""
